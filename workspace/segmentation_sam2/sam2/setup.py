from setuptools import setup, find_packages

setup(
    name="sam2",
    version="0.0.1",
    packages=find_packages(),
    include_package_data=True,
    description="Bundled SAM2 package for segmentation_sam2",
)
